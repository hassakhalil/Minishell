//notes
//the parser parse the in redir like his parsing an executing node
//the parser parse the 2 out redir like there is only one

//cat | cat | cat | ls

//<< fix the here doc
// fix file_name< file_name grep c
